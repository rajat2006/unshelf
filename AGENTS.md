# unshelf — agent config

This project uses **`CLAUDE.md`** as the canonical agent configuration. See `CLAUDE.md` for the `## Agent skills` block and `docs/agents/*.md` for the details (issue tracker, triage labels, domain docs).
