# The same Clerk publishable key as apps/api — exposed to the browser build.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
# Non-secret local feature flag. Deployed builds use DISCOVER_ENABLED at runtime.
VITE_DISCOVER_ENABLED=true
