// Vite development intentionally leaves deployment configuration unset so the
// local VITE_DISCOVER_ENABLED value controls the app. The deployed container
// overwrites this file with an explicit, fail-closed runtime value at startup.
