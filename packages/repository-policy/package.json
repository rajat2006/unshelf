{
  "name": "@unshelf/repository-policy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "unshelf-release-policy": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format esm --clean",
    "lint": "eslint src test",
    "lint:fix": "eslint src test --fix",
    "test:product": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
